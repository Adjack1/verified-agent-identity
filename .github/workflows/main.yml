name: Verify Human Identity

on:
  push:
  workflow_dispatch:
    inputs:
      agent_name:
        description: 'Agent Name'
        required: true
        default: 'rikudolaw'
      agent_description:
        description: 'Agent Description'
        required: true
        default: 'rikudolaw AI Agent'

jobs:
# ... (ke bawahnya biarin aja sama persis kayak tadi, gak usah diubah)
